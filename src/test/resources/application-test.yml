spring:
  datasource:
    url: jdbc:mysql://localhost:3306/motify_test?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
    username: root
    password: 123456789
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2-console

server:
  port: 8080
